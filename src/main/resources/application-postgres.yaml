spring:
  config.activate.on-profile: postgres
  datasource:
    url: jdbc:postgresql://localhost:5432/user_db?currentSchema=public
    username: srivatsan
    password: password
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 30
      minimum-idle: 15
      pool-name: postgres-con
      auto-commit: false